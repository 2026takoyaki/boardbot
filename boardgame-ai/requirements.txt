# 비전
ultralytics
mediapipe
opencv-python
numpy

# 트래킹
lap

# 백엔드
fastapi
uvicorn[standard]
websockets

# 오디오. TTS는 Typecast REST API를 쓴다(audio/tts/typecast.py).
httpx>=0.27
aiofiles>=23.0

# 에이전트 LLM. 없으면 네 에이전트가 전부 고정 멘트로만 말한다
# (agents/tools/llm.py가 클라이언트 생성에 실패하고 조용히 폴백한다).
openai>=1.0

# 조명. 설치는 하되 사용은 opt-in이다 — LIGHT_DRIVER=yeelight 일 때만 쓰이고,
# 기본값(mock/frontend)에서는 import조차 하지 않는다.
yeelight

# 유틸
python-dotenv
pydantic

# 개발 도구
pytest>=7.4
pytest-asyncio>=0.23
ruff>=0.1
black>=23.0
mypy>=1.5
